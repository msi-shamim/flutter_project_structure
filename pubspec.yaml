name: flutter_project_structure
description: Analyze, document, and visualize Flutter/Dart project structure. Generate AI-friendly context (CLAUDE.md, .ai-context/) and provide MCP server for live querying.
version: 2.0.0
homepage: https://github.com/msi-shamim/flutter_project_structure
license: MIT

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  analyzer: ^7.1.0
  path: ^1.9.1
  args: ^2.6.0
  yaml: ^3.1.3
  mcp_dart: ^2.1.0
  watcher: ^1.1.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.14

executables:
  flutter_project_structure: flutter_project_structure
